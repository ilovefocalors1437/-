{
  "name": "blink-to-speak",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "npx --yes serve ."
  },
  "engines": {
    "node": ">=20"
  }
}

