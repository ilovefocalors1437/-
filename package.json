{
  "name": "blink-to-speak",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:browser": "npm run build:web && node tools/smoke-supabase.js",
    "serve": "npm run build:web && npx --yes serve dist -l 4173",
    "build:web": "node tools/build-web.js"
  },
  "engines": {
    "node": ">=20"
  }
}
